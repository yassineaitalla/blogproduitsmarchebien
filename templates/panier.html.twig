{% include 'navbar.html.twig' %} 
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon panier</title>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center align-items-center" style="height: 30vh;">
            <div class="col-md-8">
                <div class="table-container">
                    <h1 class="text-center mb-4">Panier</h1>
                    <table class="table table-striped">
                        <thead class="thead-dark">
                            <tr>
                                
                                <th scope="col">Produit</th>
                                <th scope="col">Quantite</th>
                                <th scope="col">Prix  </th>
                              
                            </tr>
                        </thead>
                        <tbody>
                            {% for panier in panier %}
                            <tr>
                               
                                <td>{{ panier.getIdProduit().getNom() }} </td>
                                <td>{{ panier.quantite }}</td>
                                <td>{{ panier.total }}</td>
                               
                                
           

                            </tr>

                            

                            

                            
                            {% endfor %}
                        </tbody>

                       

                        <tr>
                            <td colspan="2" class="text-right"><strong>Total TTC:</strong></td>
                            <td class="text-right">{{ sommeTotal }} €</td>
                        </tr>

                        
                        
                        
                    </table>
                    

                 
                    
                {% if message is not empty %}
    <p>{{ message }}</p>
{% endif %}


{% if afficherPasserCommande %}
        <a  class="btn btn-primary">Passer commande</a>

        <a  class="btn btn-primary">Vider le panier</a>

        {% if afficherDevis %}
                    <a href="{{ path('demanderdevis') }}" class="btn btn-primary" style="margin-left: 250px;">Demander un devis</a>
                {% endif %}

                

    {% endif %}
                

                </div>
            </div>
        </div>
    </div>
  
</body>
</html>
